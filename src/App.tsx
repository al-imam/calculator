import Button from "./Button";

function App() {
  return (
    <main>
      <section>
        <p>10000 + {/* demo text */}</p>
        <p>10000 {/* demo text */}</p>
      </section>
      <Button click={() => {}} value="0" />
    </main>
  );
}

export default App;
